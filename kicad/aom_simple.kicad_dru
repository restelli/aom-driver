(version 1)
(rule "class 1:0"
  (condition "A.NetClass == 'USB' && B.NetClass == 'Default'")
  (constraint clearance (min 0.3048mm)))

(rule "class 1:1"
  (condition "A.NetClass == 'USB' && B.NetClass == 'USB'")
  (constraint clearance (min 0.2032mm)))
